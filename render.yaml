services:
  - type: web
    name: whop-lead-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:MBPOIuFC8xZNLBCF@db.zabxsqmxpswhjxziyzix.supabase.co:5432/postgres
      - key: PYTHON_VERSION
        value: 3.11.5
      - key: JWT_SECRET
        generateValue: true
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
